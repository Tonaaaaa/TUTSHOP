@using Microsoft.AspNetCore.Identity
@using TUTSHOP.Models.Entities

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<head>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

@if (SignInManager.IsSignedIn(User))
{
<<<<<<< HEAD
    <a id="manage" class="" asp-area="Identity" asp-page="/Account/Manage/Index" title="Quản lý" style="color: white;">Hello @UserManager.GetUserName(User)!</a>
    @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
    {
        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Admin" asp-controller="AdminHome" asp-action="Index">
                <i class="fas fa-user-cog"></i> Admin
            </a>
        </li>
    }
    <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
        <ul class="nav navbar-nav navbar-right">
            <li style="display: inline-block; margin-right: 10px;">
                <a class="" id="logout" asp-area="Identity" asp-page="/Account/Login">
                    <i class="fas fa-sign-out-alt"></i> Logout
=======

@* <a id="manage" class="" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage" style="color: white;">Hello @UserManager.GetUserAsync(User).Result.FullName!</a>


    @if (SignInManager.IsSignedIn(User) && User.IsInRole("Admin"))
    {
        <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="Admin" asp-controller="AdminHome" asp-action="Index">Admin</a>
        </li>
    }

    <li>
        <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
            <ul class="nav navbar-nav navbar-right">

                <li>
                    <a class="" id="logout" asp-area="Identity" asp-page="/Account/Login">Logout</a>

                </li>
            </ul>
        </form>
    </li> *@

    <ul class="navbar-nav ml-auto d-flex align-items-center">
        @if (SignInManager.IsSignedIn(User))
        {
            <li class="nav-item">
                <a id="manage" class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage" style="color: white;">
                    Hello @UserManager.GetUserAsync(User).Result.FullName!
>>>>>>> e0ac765eb2f95895fa1feb7dd6909cf58db59e11
                </a>
            </li>

            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item">
                    <a id="admin" class="nav-link text-dark" asp-area="Admin" asp-controller="AdminHome" asp-action="Index">Admin</a>
                </li>
            }

            <li class="nav-item">
                <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })" method="post" id="logoutForm" class="form-inline">
                    <button type="submit" class="btn btn-link nav-link" style="color: white; text-decoration: none;">Logout</button>
                </form>
            </li>
        }
    </ul>
    
    }
else
{
    <ul class="nav navbar-nav navbar-right">
        <li style="display: inline-block; margin-right: 10px;"><a class="bi bi-person-fill-add" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a></li>
        <li style="display: inline-block;"><a class="bi bi-person-circle" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a></li>
    </ul>
}
<style>
    .navbar-nav > li > a {
        padding: 15px 20px; /* Điều chỉnh kích thước của nút */
    }

        .navbar-nav > li > a i {
            margin-right: 5px; /* Điều chỉnh khoảng cách giữa biểu tượng và văn bản */
        }
</style>